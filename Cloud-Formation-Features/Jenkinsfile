pipeline {
    agent any
      stages {
          stage("CreateStack"){
	    steps {
               sh "aws cloudformation create-stack --stack-name CD8 --template-body file://Cloud-Formation-Features/sample.json   --region 'us-east-1'"
	       }
	    }

}
}


